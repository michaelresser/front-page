/* project__modal controllers */
export default class ProjectsModalController {
    constructor(event) {
       /*  this.elements = {
            target: event.target,
            modal: document.querySelector('.projects__modal'),
            modal__close: document.querySelector('.modal__close'),
            modal__name: this.target.querySelector('.modal__name'),
        };

        this.data = JSON.parse(event.target.getAttribute('data-content')); */
    }
    console.log()
    // add clik methods to all project Items
    /* document.querySelector('.projects__list').querySelectorAll('.projects__item').map((item) => item.addEventListener('click', () => alert('click'))
    
    
    handleCloseModal = () => {
        this.elements.modal.style.display = 'none';
    } */

}